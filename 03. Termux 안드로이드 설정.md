---
type: note
aliases:
  - Termux 안드로이드 설정
  - 안드로이드 Claude Code 환경
author:
  - "[[이상민]]"
date created: 2026-04-11
date modified: 2026-04-11
CMDS: "[[📚 907 Technology & Development Division]]"
index: "[[00. 🏛 Claude Code 모바일 개발 인덱스]]"
status: inProgress
tags:
  - termux
  - android
  - ssh
  - mobile-development
---

# Termux 안드로이드 설정

> 안드로이드 스마트폰에서 Termux로 Claude Code 환경에 접속하기 위한 실전 가이드.
> 2026-04-11 실제 설치 경험 기록.

## 앱 선택 (3가지 중 택 1)

| 앱 | 특징 | 추천 |
|---|------|------|
| **Termux** (무료) | 로컬 터미널 + SSH, 가장 강력 | ⭐ 개발자 추천 |
| **JuiceSSH** (무료) | SSH 전용, UI 깔끔 | 빠른 접속만 |
| **Termius** (무료/유료) | 멀티디바이스 동기화 | 여러 기기 |

---

## Termux 설치 (추천)

### 중요: 출처

Google Play 버전은 **업데이트 중단됨**. 반드시 F-Droid에서 설치:

```
https://f-droid.org/packages/com.termux/
```

또는 GitHub Releases:

```
https://github.com/termux/termux-app/releases
```

### 초기 설정 (최초 1회)

```bash
# 1. 패키지 업데이트
pkg update && pkg upgrade -y

# 2. SSH 클라이언트 설치
pkg install openssh -y

# 3. SSH 키 생성 (Enter 3번 — 기본 경로, passphrase 없음)
ssh-keygen -t ed25519 -C "android-termux"

# 4. 공개키 확인
cat ~/.ssh/id_ed25519.pub
```

공개키 (`ssh-ed25519 AAAA...`)를 Mac의 `~/.ssh/authorized_keys`에 추가.

### SSH Config 설정

```bash
mkdir -p ~/.ssh

cat > ~/.ssh/config << 'EOF'
Host studio
  HostName m4-studio
  User leesangmin
  ServerAliveInterval 30
  ServerAliveCountMax 5
EOF
```

> ⚠️ 함정: heredoc에서 `EOF` 앞에 공백 있으면 닫히지 않음. 반드시 줄 맨 앞.

이제 `ssh studio` 한 줄로 접속.

---

## Tailscale 설치 (권장)

Tailscale은 Termux 패키지가 **아니라** Google Play의 **별도 앱**.

```
Google Play → "Tailscale" 검색 → 설치 → 로그인
```

같은 계정의 Mac이 있으면 자동으로 연결됨. Tailscale hostname으로 SSH 가능.

---

## Termux 키보드 개선

### Enter 키 추가 (기본에 없음)

```bash
mkdir -p ~/.termux

cat > ~/.termux/termux.properties << 'EOF'
extra-keys = [['ESC','/','-','HOME','UP','END'],['TAB','CTRL','ALT','LEFT','DOWN','RIGHT','ENTER']]
EOF

termux-reload-settings
```

→ 하단 키바에 **ENTER 키** 추가됨.

### tmux 마우스 스크롤

Mac 쪽에서 설정 (SSH 접속 후):

```bash
cat > ~/.tmux.conf << 'EOF'
set -g mouse on
set -g history-limit 5000
set -g default-terminal "screen-256color"
EOF

tmux kill-server 2>/dev/null || true
```

이후 `cl "..."` 실행하면 손가락 스와이프로 스크롤 가능.

---

## Termux 조작 꿀팁

### 키 조합

- **볼륨 다운 + C** = Ctrl+C
- **볼륨 다운 + D** = Ctrl+D
- **볼륨 다운 + [** = tmux 복사모드 진입
- **볼륨 다운 + K/J** = 복사모드에서 위/아래 스크롤

### 화면 꺼져도 SSH 유지

알림바에서 **Acquire wakelock** 터치

### 블루투스 키보드

긴 명령은 블루투스 키보드가 훨씬 편함. 권장.

---

## 실전 흐름

```bash
# 1. Termux 열기
# 2. SSH 접속
ssh studio

# 3. 프로젝트 이동
cd ~/projects/claude-code-setup

# 4. 상태 확인 (선택)
./scripts/check-cmux-health.sh

# 5. 최신화
./scripts/sync-current-branch.sh

# 6. Claude 시작
cl "작업 내용"
```

SSH 끊기면:

```bash
ssh studio
tmux attach -t cl-claude-code-setup
```

---

## 트러블슈팅

| 증상 | 원인 | 해결 |
|------|------|------|
| `ssh: Could not resolve hostname` | Tailscale 미연결 or DNS 문제 | Tailscale 앱 확인, 또는 IP 직접 사용 |
| `cl not found` | SSH 비대화형 셸 | `ssh` 접속 후 대화형 프롬프트에서 실행 |
| tmux 스크롤 안 됨 | mouse on 미설정 | `~/.tmux.conf`에 `set -g mouse on` |
| Enter 키 없음 | Termux 기본 키바 | `termux.properties`에 extra-keys 설정 |

## 연결

- [[02. 5단계 운영 플로우]]
- [[04. 내 경험 기록]]
- [[05. 트러블슈팅 노트]]
