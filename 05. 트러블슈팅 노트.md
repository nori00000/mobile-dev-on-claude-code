---
type: note
aliases:
  - 모바일 개발 트러블슈팅
  - Claude Code 문제 해결
author:
  - "[[이상민]]"
date created: 2026-04-11
date modified: 2026-04-11
CMDS: "[[📚 907 Technology & Development Division]]"
index: "[[00. 🏛 Claude Code 모바일 개발 인덱스]]"
status: inProgress
tags:
  - troubleshooting
  - claude-code
  - tmux
  - ssh
---

# 트러블슈팅 노트

> 발생한 문제와 해결 방법을 증상 중심으로 기록.
> 새 문제가 생길 때마다 맨 위에 추가.

## 증상: `cl` 실행 시 빈 셸만 열림 (claude 미실행)

**날짜**: 2026-04-11
**환경**: 스마트폰 Termux SSH → M4 Studio → cl
**원인**: 이전 `_cl_tmux_wrap` 함수가 `CL_NO_TMUX=1 $SHELL`만 실행하고 claude를 기동하지 않음
**해결**:
- `install-shell-integration.sh`의 `_cl_tmux_wrap` → `_cl_maybe_tmux`로 리팩터링
- `tmux new-session -d` + `send-keys`로 `CL_NO_TMUX=1 cl <args>` 자동 주입
- zsh `${(q)}`로 인자 안전 이스케이프
- `funcstack[1]`로 호출자 함수명 자동 감지

**재발 방지**: `install-shell-integration.sh` 재설치 후 반드시 `zsh -i -c 'type cl'`로 검증

---

## 증상: 홈 디렉토리에서 `cl` 실행 시 `cl-leesangmin` 세션 생성

**날짜**: 2026-04-11
**원인**: `basename $PWD` → 홈에서 실행하면 `leesangmin`
**해결**: 항상 `cd ~/projects/<프로젝트>` 후 cl 실행
**개선 아이디어**:
- `cl` 함수가 git repo 루트로 자동 이동하도록 수정?
- 홈 디렉토리에서 경고 메시지?

---

## 증상: `cl not found` (SSH 비대화형)

**날짜**: 2026-04-11
**원인**: `ssh studio "cl xyz"` 같은 비대화형 실행은 `.zshrc`를 읽지 않음
**해결**: 대화형 SSH (`ssh studio`)만 사용. 접속 후 프롬프트에서 cl 실행
**대안**: `.zshenv`에 cl 함수를 이동 (비추천, 성능 영향)

---

## 증상: Termux에서 tmux 스크롤 불가

**날짜**: 2026-04-11
**원인**: tmux 기본 mouse 모드 off
**해결**:
```bash
cat > ~/.tmux.conf << 'EOF'
set -g mouse on
set -g history-limit 5000
set -g default-terminal "screen-256color"
EOF
tmux kill-server
```

**보조**: 복사 모드 진입 (볼륨다운+[) 후 K/J로 라인 스크롤

---

## 증상: Termux에 Enter 키 없음

**날짜**: 2026-04-11
**원인**: Termux 기본 extra-keys에 ENTER 미포함
**해결**:
```bash
mkdir -p ~/.termux
cat > ~/.termux/termux.properties << 'EOF'
extra-keys = [['ESC','/','-','HOME','UP','END'],['TAB','CTRL','ALT','LEFT','DOWN','RIGHT','ENTER']]
EOF
termux-reload-settings
```

---

## 증상: `cat > file << 'EOF'` heredoc이 닫히지 않음

**날짜**: 2026-04-11
**원인**: `EOF` 앞에 공백이나 들여쓰기 있음
**해결**: 반드시 **줄 맨 앞**에 `EOF` 작성. 복사-붙여넣기 시 주의
**탈출**: 이미 들어간 경우 Ctrl+C (볼륨다운+C)

---

## 증상: 커밋 메시지에 `git checkout -- .` 같은 텍스트 포함 시 hook이 차단

**날짜**: 2026-04-11
**원인**: deny-destructive-commands.py가 `tool_input.command` 전체를 regex 검사 → 커밋 메시지 내용까지 검사
**해결**: 커밋 메시지에서 위험 패턴 텍스트 제거 (예: `force` → `force push`, `checkout --` → `checkout 위험패턴`)
**교훈**: hook이 정상 작동한다는 반증이기도 함

---

## 증상: `pkg install tailscale` Error

**날짜**: 2026-04-11
**원인**: Tailscale은 Termux 패키지가 아니라 Android 별도 앱
**해결**: Google Play Store에서 Tailscale 앱 설치 → 로그인

---

## 새 증상 기록 템플릿

### 증상: ...

**날짜**: YYYY-MM-DD
**환경**: (어떤 Mac, 어떤 앱에서)
**원인**:
**해결**:
**재발 방지**:

---

## 연결

- [[04. 내 경험 기록]]
- [[03. Termux 안드로이드 설정]]
- [[00. 🏛 Claude Code 모바일 개발 인덱스]]
