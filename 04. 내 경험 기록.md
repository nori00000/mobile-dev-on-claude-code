---
type: note
aliases:
  - 모바일 개발 경험 로그
author:
  - "[[이상민]]"
date created: 2026-04-11
date modified: 2026-04-11
CMDS: "[[📚 907 Technology & Development Division]]"
index: "[[00. 🏛 Claude Code 모바일 개발 인덱스]]"
status: inProgress
tags:
  - experience
  - log
  - claude-code
  - mobile-development
---

# 내 경험 기록

> 실제 사용하면서 발견한 것, 실수한 것, 깨달은 것.
> 시간순으로 추가. 나중에 패턴화해서 교육자료로 이전.

## 2026-04-11 — 첫 Termux 설치

### 한 것

- Termux 설치 (Play Store에서)
- `pkg update && pkg upgrade`
- `pkg install openssh`
- `ssh-keygen -t ed25519` (passphrase 없이)
- Mac의 `~/.ssh/authorized_keys`에 공개키 추가
- `ssh studio` 접속 성공
- 프로젝트 이동 → `cl "프로젝트 현황 알려줘"` 실행

### 잘된 것

- SSH 접속은 한 번에 성공
- Tailscale hostname으로 접속 OK
- `cl` 호출 시 tmux 세션 자동 생성 확인

### 막힌 것

- **문제**: Termux heredoc에서 `EOF` 앞에 공백이 있어서 파일이 닫히지 않음
- **해결**: 복사-붙여넣기 시 들여쓰기 주의. 줄 맨 앞에 `EOF` 와야 함

- **문제**: tmux 진입 후 화면을 위로 스크롤할 수 없음
- **해결**: `~/.tmux.conf`에 `set -g mouse on` 설정

- **문제**: Enter 키가 화면에 없어서 명령 실행 불편
- **해결**: `~/.termux/termux.properties`에 extra-keys 설정

- **문제**: 스마트폰에서 cl 실행 시 빈 셸만 열리고 claude가 시작되지 않음
- **원인**: 이전 버전 `_cl_tmux_wrap`이 `CL_NO_TMUX=1 $SHELL`만 실행
- **해결**: `_cl_maybe_tmux`로 리팩터링 + `send-keys`로 claude 자동 실행

### 배운 것

1. tmux 세션명이 `basename $PWD` 기반이라 홈 디렉토리(`~`)에서 cl 실행하면 `cl-leesangmin` 같은 무의미한 이름 생성
   → 항상 프로젝트 디렉토리에서 실행해야 함
2. SSH 비대화형 셸은 `.zshrc`를 읽지 않음 → `cl` 함수를 못 찾음
   → `ssh studio`로 대화형 접속한 뒤 프롬프트에서 실행
3. `pkg install tailscale` 안 됨 → Tailscale은 별도 Android 앱

---

## 2026-04-11 — 5단계 운영 플로우 정립

### 배경

여러 Mac과 스마트폰을 오가며 개발하는 워크플로를 체계화할 필요성 느낌.

### 결정한 것

- **스마트폰**: plain SSH 전용. wrapper/cmux 같은 로컬 도구 의존 최소화
- **다른 Mac**: cmux/wrapper 사용 가능
- **handoff**: 현재 브랜치 기준 (`git fetch → switch → pull --ff-only`)
- **fallback**: cmux 불가 시 `CL_NO_TMUX=1 cl`

### 만든 스크립트 (codex-setup에서 이식)

- `check-cmux-health.sh` — 0/10/20 exit code로 건강 상태
- `sync-current-branch.sh` — 현재 브랜치 자동 감지 + 안전한 동기화
- `ssh-main-mac-project.sh` — 기준 Mac SSH + 프로젝트 이동 wrapper

---

## 앞으로 기록할 것

- [ ] 실제 스마트폰에서 작업 완수한 첫 이슈
- [ ] cmux 끊김 복구 경험
- [ ] 여러 Mac 간 handoff 시 발생한 git 충돌 사례
- [ ] 긴 작업을 스마트폰에서 진행했을 때 배터리/발열
- [ ] Termux 백그라운드 유지 팁
- [ ] SSH 키 관리 (여러 기기)

---

## 패턴 후보 (교육자료로 발전 예정)

### 패턴 1: "프로젝트 중심 세션명"
- 반복해서 발견: 홈에서 cl 실행하면 의미없는 세션 생성
- 교훈: `cd <프로젝트>` 후 `cl`이 원칙
- 후속: init-project.sh의 blueprint별 자동화?

### 패턴 2: "2중 핸드오프 동기화"
- dev-retrospective 경로 (session-backup) + project-profile.md 양쪽
- `.claude/` gitignore에 포함돼도 1순위 경로로 작동

## 연결

- [[00. 🏛 Claude Code 모바일 개발 인덱스]]
- [[05. 트러블슈팅 노트]]
- [[01. 핵심 개념 — tmux, cl, clp]]
