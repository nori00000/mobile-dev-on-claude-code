---
type: note
aliases:
  - 5단계 운영 플로우
  - 멀티머신 개발 시나리오
author:
  - "[[이상민]]"
date created: 2026-04-11
date modified: 2026-04-11
CMDS: "[[📚 907 Technology & Development Division]]"
index: "[[00. 🏛 Claude Code 모바일 개발 인덱스]]"
status: inProgress
tags:
  - workflow
  - claude-code
  - handoff
  - mobile-development
---

# 5단계 운영 플로우

> 스마트폰/여러 Mac 간 개발 이동 시 사용하는 표준 워크플로우.

## 큰 그림

당신은 **3대의 Mac**과 **스마트폰**으로 개발. 장소를 옮기면서 작업을 이어가고 싶음.

### 필요한 것 3가지

1. **같은 Claude Code 환경** — 어느 Mac에 가든 똑같이 작동
2. **작업 상태 공유** — 내가 어디까지 했는지 다른 Mac도 앎
3. **끊김 대응** — SSH/cmux 끊겨도 작업 살아있음

## 5단계 요약표

| # | 시나리오 | 명령 |
|---|----------|------|
| 1 | 평소 작업 시작 | `cl "작업"` |
| 2 | 다른 Mac에서 이어받기 | `~/claude-code-setup/scripts/sync-current-branch.sh && cl "작업"` |
| 3 | 스마트폰에서 | `ssh studio && cd <프로젝트> && ~/claude-code-setup/scripts/check-cmux-health.sh && cl` |
| 4 | cmux 불가 시 | `CL_NO_TMUX=1 cl "작업"` |
| 5 | Mac 간 handoff | `~/claude-code-setup/scripts/sync-current-branch.sh` 후 새 Mac에서 `cl` |

---

## 시나리오 1 — 집에서 평소처럼 작업

M4 Studio 앞에서:

```bash
cd ~/projects/내프로젝트
cl "API 엔드포인트 추가해줘"
```

**끝.** tmux 세션이 자동 생성되고 Claude가 작업 시작.

---

## 시나리오 2 — 카페에서 MacBook Air로 이어받기

```bash
# 1. 프로젝트 이동
cd ~/projects/내프로젝트

# 2. 집에서 작업한 내용 가져오기
~/claude-code-setup/scripts/sync-current-branch.sh

# 3. 작업 이어가기
cl "아까 만들던 API에 에러 처리 추가"
```

Claude가 `[handoff] m4-studio → 내프로젝트 (어제 20:30)` 메시지를 먼저 보여줌.

---

## 시나리오 3 — 스마트폰 긴급 수정 (제일 중요)

지하철에서 버그 제보가 왔을 때:

```bash
# 1. 집 Mac에 SSH 접속
ssh studio

# 2. 프로젝트 이동
cd ~/projects/내프로젝트

# 3. 건강 체크
~/claude-code-setup/scripts/check-cmux-health.sh

# 4. 최신 상태로 맞추기
~/claude-code-setup/scripts/sync-current-branch.sh

# 5. Claude 시작
cl "로그인 버그 수정"
```

지하철 신호 끊겨도 tmux 덕분에 Claude는 안 죽음. 다시 접속 후:

```bash
ssh studio
tmux attach -t cl-내프로젝트
```

---

## 시나리오 4 — cmux가 고장났을 때

```bash
CL_NO_TMUX=1 cl "작업 내용"
```

앞에 `CL_NO_TMUX=1`만 붙이면 tmux 없이 직접 실행. 단점: SSH 끊기면 작업도 끊김. 짧은 작업에만.

---

## 시나리오 5 — Mac 간 "주도권" 넘기기

**Studio에서 (보내기 전)**:
```bash
git add -A
git commit -m "WIP: 오늘 작업"
git push origin $(git branch --show-current)
```

**M1 Pro에서 (받기)**:
```bash
cd ~/projects/내프로젝트
~/claude-code-setup/scripts/sync-current-branch.sh
cl "어제 Studio에서 하던 거 이어서"
```

---

## 황금 규칙 3가지

### 규칙 1: 프로젝트 디렉토리에서 실행

```bash
# ❌ 나쁜 예 — 홈 디렉토리에서
cd ~
cl "작업"   # tmux 세션 이름이 cl-leesangmin (의미 없음)

# ✅ 좋은 예
cd ~/projects/내프로젝트
cl "작업"   # tmux 세션 이름이 cl-내프로젝트 (명확)
```

### 규칙 2: Mac 떠나기 전엔 git push

```bash
git add -A
git commit -m "WIP"
git push origin $(git branch --show-current)
```

### 규칙 3: 뭔가 이상하면 health check

```bash
cd ~/claude-code-setup
./scripts/check-cmux-health.sh
```

Exit code:
- **0** = healthy (cl 바로 가능)
- **10** = fallback-ready (cmux 불가, `CL_NO_TMUX=1 cl` 사용)
- **20** = claude unhealthy (설치 확인 필요)

---

## 헬퍼 스크립트

| 스크립트 | 기능 |
|----------|------|
| `check-cmux-health.sh` | cmux + claude 건강 체크 |
| `sync-current-branch.sh` | 현재 브랜치 fetch+switch+pull --ff-only |
| `ssh-main-mac-project.sh` | 기준 Mac SSH + 프로젝트 자동 이동 |

## 연결

- [[01. 핵심 개념 — tmux, cl, clp]]
- [[03. Termux 안드로이드 설정]]
- [[04. 내 경험 기록]]
