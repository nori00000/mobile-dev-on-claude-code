---
type: note
aliases:
  - tmux cl clp 개념
  - Claude Code 별칭 설명
author:
  - "[[이상민]]"
date created: 2026-04-11
date modified: 2026-04-11
CMDS: "[[📚 907 Technology & Development Division]]"
index: "[[00. 🏛 Claude Code 모바일 개발 인덱스]]"
status: inProgress
tags:
  - tmux
  - claude-code
  - shell
  - concept
---

# 핵심 개념 — tmux, cl, clp

> 초보자를 위한 친절한 설명. 나중에 교육자료로 다듬을 원본.

## tmux가 뭐야?

**tmux = "터미널 안에 터미널을 넣는 컨테이너"**

### 비유

사무실에서 문서 작업을 하다가 **사무실 문을 잠그고 나간다**고 상상:

- **tmux 없이**: 문을 잠그면 책상 위 모든 게 치워짐. 다음에 돌아오면 처음부터 다시.
- **tmux 있이**: 문을 잠가도 책상은 그대로. 다음에 돌아오면 하던 것 그대로 이어서.

### 실제 상황

```
SSH로 Mac에 접속해서 Claude로 1시간짜리 작업 중
갑자기 Wi-Fi 끊김!

tmux 없으면: Claude 죽음. 1시간 작업 날아감.
tmux 있으면: Claude는 Mac에서 계속 돌아감.
            Wi-Fi 복구 후 다시 접속하면 그대로 보임.
```

### 실제로 보면

`cl "작업"` 실행하면 화면이 이렇게 바뀜:

```
┌──────────────────────────────────────────────┐
│                                              │
│  Claude Code가 여기서 작업 중...              │
│                                              │
├──────────────────────────────────────────────┤
│ [cl-내프로젝트] 0:0-claude  "main-mac"      │ ← 초록 줄 = tmux 신호
└──────────────────────────────────────────────┘
```

맨 아래 **초록색 줄**이 tmux 신호. 이 줄이 보이면 "끊겨도 안전하다" 라고 생각하면 됨.

### tmux 핵심 명령어 3개

```bash
tmux ls                          # 세션 목록 보기
tmux attach -t cl-내프로젝트      # 세션에 돌아가기
# Ctrl+B → D                    # 세션 나가기 (죽이지 않음)
```

---

## cl vs clp — 차이가 뭐야?

**한 줄 요약**: `cl`은 바로 실행, `clp`는 먼저 물어봄.

### cl — "그냥 해줘" 모드

```bash
cl "로그인 버그 고쳐줘"
```

→ Claude가 **바로** 파일 수정 시작.

**언제 쓰나**: 명확한 작업, 빠른 수정

**예시**:
- `cl "README 오타 수정"`
- `cl "404 페이지에 돌아가기 버튼 추가"`
- `cl "이 함수에 에러 처리 추가"`

### clp — "제안부터 보여줘" 모드 (p = proposal)

```bash
clp "로그인 시스템 개선"
```

→ Claude가 구현 전에 **3가지 선택지** 먼저 제시.

**Claude 응답 예시**:

```
1. 현재 이해
   - 로그인 시스템을 개선하고 싶어함

2. 선택지
   1안 (최소): 에러 메시지만 개선
   2안 (추천): 세션 타임아웃 + 비밀번호 정책 추가
   3안 (확장): OAuth 소셜 로그인까지 추가

3. 쉬운 설명
   1안 — UI만 다듬는 수준
   2안 — 보안 + UX 개선, 2시간 작업
   3안 — 전면 리팩토링, 반나절 작업

4. 장단점
   1안: 쉽지만 효과 미미
   2안: 밸런스 좋음
   3안: 크지만 미래 대비

5. 추천
   → 2안. 지금 보안 구멍이 있어서 시급함.

6. 짧은 질문
   1/2/3 중 어떤 걸로 진행할까요?
```

`2` 라고 답하면 그때부터 구현 시작.

**언제 쓰나**: 애매한 작업, 중요한 결정, 방향부터 잡고 싶을 때

### 비교표

| | `cl` | `clp` |
|---|------|-------|
| 뜻 | "그냥 해" | "제안부터 해" |
| 속도 | 빠름 | 한 단계 느림 |
| 언제 | 명확할 때 | 애매할 때 |
| 결과 | 바로 코드 변경 | 먼저 3가지 안 제시 |
| 비유 | 택시 "공항까지" | 여행사 "3가지 코스 중 추천" |

---

## 나머지 별칭: clr, clf, cli

### clr — 리뷰 모드 (r = review)

Claude가 코드를 **절대 바꾸지 않고** 오직 **검토만** 함.

```bash
clr "이 PR에 버그 있는지 봐줘"
```

- 잠재적 버그 지적
- 테스트 누락 지적
- 리팩토링 제안
- **코드 수정 없음**

**언제**: 남의 PR 리뷰, 내 작업 double-check

### clf — 피드백 저장 (f = feedback)

Claude 응답이 좋았을 때/나빴을 때 기록.

```bash
clf 5 helpful clear "제안 모드가 방향 잡기에 유용했다"
```

- `5` — satisfaction 1-5점
- `helpful` — helpfulness (helpful|neutral|not_helpful)
- `clear` — clarity (clear|mixed|unclear)
- `"..."` — 자유 코멘트

저장 위치: `~/.claude/feedback/<YYYY-MM>.jsonl` (clf 첫 실행 시 디렉토리 자동 생성, 현재 월 기준)

**초보자는 안 써도 됨.** 익숙해지면 기록용.

### cli — 직접 실행 (i = instant)

Claude CLI를 기본 설정 없이 그대로 실행.

```bash
cli --version
cli
```

- `cl`의 안전 프롬프트 없음
- `cl`처럼 tmux 래핑은 적용됨
- 가끔 CLI 직접 옵션이 필요할 때

---

## 처음이면 이 3개만 외우면 됨

```bash
cl   "작업 내용"    # 95% 이걸 씀
clp  "애매한 작업"  # 가끔 씀
tmux ls             # 어떤 세션이 살아있는지 확인
```

## 연결

- [[02. 5단계 운영 플로우]]
- [[03. Termux 안드로이드 설정]]
- [[claude-code-setup]]
